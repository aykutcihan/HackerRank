// Paste the HackerRank Java code template here exactly as given.
// Add `return 0;` as a placeholder inside the function body.
// The structure below is an example only — every problem is different.

import java.io.*;
import java.util.*;
import java.util.stream.*;
import static java.util.stream.Collectors.toList;

class Result {

    public static int solve(List<Integer> ar) {
        // Write your solution here
        return 0;
    }

}

public class Solution {
    public static void main(String[] args) throws IOException {
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bufferedWriter = new BufferedWriter(new FileWriter(System.getenv("OUTPUT_PATH")));

        // Read input and call Result.solve(...)

        bufferedReader.close();
        bufferedWriter.close();
    }
}
